info:
  name: Resend Vrify Email
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/auth/email-verification/resend"
  body:
    type: json
    data: |-
      {
        "email": "omae.elnadii@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
