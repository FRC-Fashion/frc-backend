info:
  name: Vrify Email
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/auth/email-verification/verify"
  body:
    type: json
    data: |-
      {
        "email": "omar.elnadi.dev@gmail.com",
        "activationCode": "958721"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
