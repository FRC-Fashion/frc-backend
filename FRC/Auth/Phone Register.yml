info:
  name: Phone Register
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/auth/phone/register"
  body:
    type: json
    data: |
      {
        "fullName": "Omar Ahmed",
        "phoneNumber": "+201013341863",
        "password": "Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
