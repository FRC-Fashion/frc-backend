info:
  name: Resend Manual Vrify Email
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/auth/email-verification/resend"
  body:
    type: json
    data: |-
      {
        "email": "omar.elnadi.dev@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
